server:
  port: 8761

application:
  title: @project.name@
  version: @project.version@

spring:
  application:
    name: eureka-server
  zipkin:
    base-url: http://localhost:9411

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
